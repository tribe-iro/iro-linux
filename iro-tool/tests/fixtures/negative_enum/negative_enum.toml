schema_version = "1.5"
set = "negative_enum"
includes = ["probe_types.h"]

[types.sample]
c_type = "struct sample_struct"
fields = ["x"]

[enums.sample]
c_type = "enum sample_enum"
extract_all = true
